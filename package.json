{
  "name": "@bin-ntc/binn-ui",
  "private": true,
  "author": "BinN <ntc.binh96@gmail.com>",
  "license": "MIT",
  "scripts": {
    "bootstrap": "pnpm install",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream",
    "publish:ci": "lerna publish from-package --yes"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.0.0",
    "pnpm": "^8.0.0",
    "typescript": "^5.0.0",
    "changesets": "^1.0.2"
  }
}